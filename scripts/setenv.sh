#! /bin/sh
# this must be run as "source ./setenv.sh"
# to pass this value to the present proccess shell window variables
# change the last part to match the path of where you installed your newdawn bin directory
export PATH="/usr/bin:/bin::./bin"
cd ~/surething/working/



